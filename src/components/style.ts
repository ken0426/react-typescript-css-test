import { theme } from './contents/theme';

export const style: { [key: string]: React.CSSProperties } = {
  red: {
    color: 'red',
    fontSize: theme.oneHundred,
  },
  blue: {
    color: 'blue',
    fontSize: theme.oneHundred,
  },
  yellow: {
    color: 'yellow',
    fontSize: theme.oneHundred,
  },
};
